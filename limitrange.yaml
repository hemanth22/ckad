apiVersion: v1
kind: LimitRange
metadata:
  name: defaults

spec:
  limits:

  - default:
      cpu: "1"
      memory: 512Mi

    defaultRequest:
      cpu: 250m
      memory: 128Mi

    type: Container
